package flow;

import domain.Menu;
import domain.Order;
import domain.Table;
import view.InputView;
import view.OutputView;

public class OrderFunction implements ChickenFunction {
	@Override
	public void run() {
		//테이블 그림 출력
		// 테이블 선택하세요 입력
		// 메뉴 전체 출력
		// 등록할 메뉴 선택, 수량 선택   [주문 객체 생성]
		// 전체 주문에 담기.
/*		OutputView.printTables(tables);
		final Table table = InputView.inputTable();

		OutputView.printMenus(menus);
		Menu menu = InputView.inputMenu();
		int menuQuantity = InputView.inputMenuQuantity();

		Order order = new Order(menu, menuQuantity);

		totalOrders.addOrder(table, order);*/
	}
}
